function LocationMap() {
  return (
    <div className="location-map">
      {/* TODO: integrar mapa real (Google Maps u otro proveedor). */}
      <div className="map-placeholder">Aquí se integrará un mapa (Google Maps) en el futuro.</div>
    </div>
  );
}

export default LocationMap;
